<h1>Users#index</h1>
<p>
  <%= link_to "New", new_user_path %>
</p>
<p>
  <ul>
    <% @users.each do |user| %>
    <li>
      <%= link_to user.name, edit_user_path(user) %>
      <%= button_to "Destroy", user_path(user), method: :delete, form_class: "inline" %>
    </li>
    <% end %>
  </ul>
</p>
